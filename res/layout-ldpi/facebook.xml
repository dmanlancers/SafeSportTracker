<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#00000000" >

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:padding="5dp" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_margin="5dip"
            android:text="Partilhar no Facebook?" android:textSize="25dp"
            android:textColor="#FFFFFF" >
        </TextView>
    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:orientation="horizontal" >
    </LinearLayout>

    <Button
        android:id="@+id/FacebookShareNotButton"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/FacebookShareButton"
        android:layout_alignBottom="@+id/FacebookShareButton"
        android:layout_alignParentRight="true" android:background="#999999"
        android:layout_margin="2dp"
        android:onClick="doNotShare"
        android:padding="5dp"
        android:text="Não"  android:textColor="#000000" android:textSize="20dp"/>

    <Button
        android:id="@+id/FacebookShareButton"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/linearLayout1"
        android:layout_margin="2dp"
        android:layout_marginTop="91dp" android:background="#999999"
        android:onClick="share"
        android:padding="5dp"
        android:text="Sim"
        android:textColor="#000000"
        android:textSize="20dp" />

</RelativeLayout>